package io.versailles.domain.model

/**
  * A player's title that is shown within the [[ContextMenu]] when presented.
  * @author Sino
  */
case class ContextMenuTitle(
  left: String,
  center: String,
  right: String
)
